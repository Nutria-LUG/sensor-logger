#ifndef DEFS_INCLUDE_GUARD_HH
#define DEFS_INCLUDE_GUARD_HH 1

namespace SENSOR_READER_INFO {
    const char* NAME = "sensor-logger";
    const char* HOME_ENV = "HOME";
    const char* CONFIG_FILE_NAME = "config";
}

#endif
